# frontDevTemplate
Startup package for prototyping/testing

INSTALL
-----------------------------
node.js
git
npm install -g gulp
git clone --bare [url] .git
git config --bool core.bare false
git reset --hard
npm install
gulp
